import React, {FC} from 'react';
import style from "./home.module.scss"

const Home:FC = () => {
    return (
        <div className={style.root}>
            home
        </div>
    );
};

export default Home;